// LOADER

export const getLoader = state => state.loader;

// MODAL

export const getModal = state => state.modal.modal;

export const getModalSignUp = state => state.modal.signup;

export const getModalLogIn = state => state.modal.login;

export const getModalBasket = state => state.modal.basket;

// USER DATA

export const getName = state => state.user.name;

export const getAutorization = state => state.user.autorization;

export const getAdress = state => state.user.adress;

// MENU

export const getMenu = state => state.menu;

// BASKET

export const getBasket = state => state.basket;
